services:
  web:
    image: web
    container_name: web
    build:
      context: .
      dockerfile: ./apps/web/Dockerfile
    ports:
      - 3000:3000
    env_file:
      - ./apps/web/.env
  # admin:
  #   image: admin
  #   container_name: admin
  #   build:
  #     context: .
  #     dockerfile: ./apps/admin/Dockerfile
  #   ports:
  #     - 3001:3001
  #   env_file:
  #     - .env
