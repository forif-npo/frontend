echo "🔍 commit 이전에 lint 규칙을 적용합니다..."
if npx lint-staged; then
  echo "✅ 모든 lint 규칙이 성공적으로 적용되었습니다."
  echo "🔍 package.json 파일을 정렬합니다."
  pnpm lint:package
  echo "✅ 모든 package.json 파일이 성공적으로 정렬되었습니다."
  exit 0
else
  echo "❌ lint 규칙 검사에서 오류가 발생했습니다."
  exit 1
fi