# 디자인 리뷰 운영 기준

이 문서는 FORIF Frontend 화면의 일관성과 정보 위계를 스스로 발견·검토하기 위한
에이전트 기준이다. 목표는 요청받은 변경을 구현하는 데 그치지 않고, 사용자에게 보이는
화면에서 근거 없는 불일치와 사용성 저하를 찾아 우선순위화하는 것이다.

## 적용 시점

- UI를 새로 만들거나 수정할 때
- UI 리팩터링 후보를 찾을 때
- 화면 출력 또는 디자인 시스템 관련 리뷰를 요청받을 때

API, 권한, 데이터 형식 같은 제품 계약을 바꾸지 않는 디자인 결함은 발견과 보고를
자율적으로 진행한다. 색상·간격·문구·variant처럼 의도나 브랜드 결정을 바꿀 수 있는
수정은 문제, 근거, 사용자 영향을 먼저 보고하고 확인을 받은 뒤 진행한다.

## 관찰 방법

코드만으로 디자인 결론을 내리지 않는다. 실행 가능한 경우 로컬 web 또는 Storybook을
브라우저로 열어 실제 렌더링을 확인한다.

1. 변경한 화면과 인접한 재사용 화면을 확인한다.
2. desktop과 mobile viewport에서 기본·로딩·빈·오류·비활성 상태 중 해당하는 상태를
   확인한다.
3. 화면의 스크린샷과 접근성 트리를 함께 확인한다. 스크린샷은 밀림, 대비, 정렬,
   정보 위계를, 접근성 트리는 heading, label, role, 키보드 조작을 확인하는 근거다.
4. 실행 환경이 없거나 인증·외부 의존성 때문에 상태를 재현하지 못하면, 확인하지 못한
   상태와 그 이유를 기록한다. 코드 추정만으로 통과시키지 않는다.

## 반응형 계약

반응형은 desktop 화면을 축소하는 작업이 아니다. 각 화면은 좁은 폭에서 사용자가
완료해야 하는 행동과 읽어야 하는 정보를 먼저 정하고, 넓은 폭에서 보조 정보와 병렬
작업을 추가한다.

### 공통 원칙

- mobile base를 기본 상태로 두고 `sm`·`md`·`lg`는 화면 폭이 늘어날 때 필요한 정보와
  공간을 추가하는 데 사용한다. desktop 전용 고정 폭을 축소 화면의 기본값으로 두지
  않는다.
- breakpoint는 기기 이름이 아니라 콘텐츠가 더 이상 읽히거나 조작될 수 없는 지점에
  둔다. 같은 역할의 컴포넌트는 같은 전환 규칙을 사용한다.
- `@repo/ui`의 mobile typography variant와 공통 `Table`·`Title` 같은 primitive를 먼저
  사용한다. 페이지마다 임의 글자 크기, 고정 폭, 서로 다른 전환 폭을 추가할 때는
  재사용할 수 없는 이유를 코드 가까이에 남긴다.
- 가로 스크롤은 데이터 비교가 본질인 table·calendar처럼 열을 유지해야 하는 경우에만
  허용한다. 이때 고정된 첫 열, 스크롤 가능성의 단서, 키보드 접근을 함께 검토한다.
- mobile에서 숨기는 정보는 보조 정보여야 한다. 핵심 행동·선택 상태·오류·필수 입력은
  숨기지 않고, 순서를 바꾸거나 별도 제어로 제공한다.

### 화면별 결정 기록

UI를 새로 만들거나 반응형 결함을 수정할 때 아래 결정을 구현 전 또는 리뷰 기록에
남긴다.

| 대상            | 좁은 폭의 표현                         | 넓은 폭의 표현 | 보존할 핵심 행동       |
| --------------- | -------------------------------------- | -------------- | ---------------------- |
| navigation      | 메뉴 버튼 또는 sheet                   | 전체 메뉴      | 모든 목적지 탐색       |
| table/list      | 카드화, 중요 열 우선, 또는 가로 스크롤 | 전체 열        | 행 비교·상세 이동·정렬 |
| form/action row | 세로 배치 또는 전체 폭 버튼            | 인라인 배치    | 입력·오류 확인·제출    |
| card grid       | 1열과 읽기 가능한 카드 높이            | 다열 grid      | 항목 비교·선택         |
| dialog/sheet    | viewport 내 스크롤과 안전한 닫기       | 적절한 최대 폭 | 내용 확인·취소·확정    |

이 표의 선택은 예시다. 화면의 도메인상 다른 방식이 필요하면 사용자의 행동과 정보
위계로 설명한다.

### 검증 viewport

모든 UI 변경은 최소한 아래 viewport에서 확인한다.

| 구간          | viewport   | 확인 내용                                                  |
| ------------- | ---------- | ---------------------------------------------------------- |
| mobile        | 390 × 844  | 한 손 조작, 줄바꿈, 고정 요소, 메뉴·form·table의 대체 표현 |
| tablet / 전환 | 768 × 1024 | `md` 전환 직후의 grid, navigation, 행동 영역               |
| desktop       | 1440 × 900 | 최대 폭, 여백, 병렬 정보, 긴 콘텐츠                        |

화면이 `sm`·`md`·`lg`에서 구조를 전환한다면 전환 폭의 바로 전과 후도 확인한다. 예를
들어 `md` 전환은 767px과 768px에서 모두 확인한다. viewport는 특정 기기 지원 선언이
아니라 레이아웃 붕괴를 조기에 발견하기 위한 공통 검토 기준이다.

## 반응형 개선 순서

PC 우선으로 만들어진 기존 화면을 한 번에 바꾸지 않는다. 아래 순서로 사용자 여정별로
개선한다.

1. 라우트별로 overflow, 잘림, 조작 불가, 정보 위계 붕괴를 browser에서 수집하고 P1/P2/P3를
   정한다.
2. 반복되는 원인이 있는 공통 primitive부터 수정한다. 우선 대상은 page container,
   navigation, page header, title/body typography, action row, form field, table, card grid,
   dialog/sheet다.
3. 회원가입·로그인·스터디 탐색·스터디 신청처럼 핵심 사용자 여정을 P1부터 한 화면씩
   적용하고, 해당 상태와 viewport를 재검증한다.
4. 공통 primitive가 안정된 뒤 개별 화면의 임의 폭과 중복 breakpoint를 정리한다.
5. 발견 사항, 전환 규칙, 검증 screenshot을 축적해 다음 UI 변경의 기준으로 사용한다.

## 검토 기준

### 일관성

- 같은 역할의 텍스트, 아이콘, 버튼, 입력, 카드, 테이블 셀은 동일한 component
  variant와 디자인 토큰을 사용하는가?
- 글자 크기, 굵기, line-height, 색상, padding, radius, border가 달라질 때 그 차이를
  정보 위계, 데이터 유형, 상호작용 상태로 설명할 수 있는가?
- 하나의 화면에서 유사한 UI가 서로 다른 간격, 정렬, 상태 표현을 쓰고 있지 않은가?

### 정보 위계와 가독성

- 사용자가 가장 먼저 읽어야 할 제목, 주요 값, 보조 정보, 상태가 시각적으로 구분되는가?
- 긴 텍스트, 숫자, 날짜, 태그, 비어 있는 값이 잘리거나 다른 행의 정렬을 무너뜨리지
  않는가?
- 색상만으로 상태나 중요도를 전달하지 않고, 텍스트·아이콘·형태 등 보조 단서를
  제공하는가?

### 테이블과 목록

- 같은 의미의 값은 모든 행과 열에서 같은 typography와 정렬을 쓰는가?
- 특정 열만 글자 크기나 굵기가 다르면, 그 열이 보조 메타데이터·수치·상태 등으로
  다른 위계를 가져야 하는 근거가 있는가?
- 헤더와 셀의 연결, 정렬, 밀도, hover/focus/선택 상태가 일관적인가?
- 정렬·필터·페이지네이션·빈 결과가 있을 때도 읽기 순서와 조작 대상이 명확한가?

### 반응형과 접근성

- mobile에서 가로 스크롤, 잘림, 터치 대상 축소, 우선순위 없는 정보 나열이 생기지
  않는가?
- 키보드 focus가 보이고, label·role·heading 구조가 화면의 의미와 맞는가?
- 대비와 disabled/loading/error 상태가 읽을 수 있고 오해를 만들지 않는가?

## 발견 사항의 처리

발견 사항마다 다음을 남긴다.

1. 화면·컴포넌트·상태
2. 관찰한 불일치 또는 사용자 영향
3. 의도된 차이인지 판단한 근거 또는 불확실성
4. P1/P2/P3 우선순위와 최소 수정 제안
5. 확인한 viewport와 상태

P1은 핵심 행동을 막거나 내용을 읽을 수 없게 하는 경우다. P2는 근거 없는 디자인
시스템 위반, 정보 위계 붕괴, 반복되는 일관성 문제다. P3는 제품 방향을 정한 뒤 다룰
장기 개선 후보다.

명백한 토큰·component variant 위반은 수정 후보로 제안할 수 있다. 의도 판별이 필요한
차이와 브랜드 결정은 자의적으로 통일하지 않고, 근거와 선택지를 보고한다.

## 누적 디자인 냄새

아래 항목은 이후 리뷰에서 반드시 점검한다. 사용자의 피드백이나 실제 발견으로 항목을
추가하고, 해결된 항목은 해결 근거와 함께 제거한다.

| 항목                                | 점검 기준                                                                                     |
| ----------------------------------- | --------------------------------------------------------------------------------------------- |
| 근거 없는 테이블 열 typography 차이 | 특정 열의 글자 크기·굵기·행간·정렬이 다를 때, 데이터 역할에 따른 정보 위계가 있는지 확인한다. |
