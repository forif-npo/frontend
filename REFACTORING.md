# 리팩토링 운영 기준

이 문서는 FORIF Frontend 리팩토링의 판단 기준입니다.

목표는 제품 신뢰도를 유지하고 변경 비용과 회귀 위험을 낮추는 것입니다.

## 최우선 계약

유지 대상은 사용자 동작, 화면 출력, 접근성 의미, 권한, 데이터 형식, API 계약입니다.
이 항목의 변경은 기능 변경 또는 결함 수정 절차로 분류합니다.

권한·인증·환경 변수·API 계약·배포·데이터 손실 가능성이 있는 변경은 작업 전에
문제와 영향 범위를 제기하고 확인을 받습니다.

## 우선순위

| 우선순위 | 대상                                                            | 처리 원칙                                  |
| -------- | --------------------------------------------------------------- | ------------------------------------------ |
| P0       | 권한, 인증, 데이터 유실, API 계약, 배포 차단                    | 다른 작업보다 먼저 원인과 영향을 확인한다. |
| P1       | 실패 상태, 비동기 경합, 핵심 사용자 액션 오류, 위험한 검증 공백 | 작은 경계로 수정하고 해당 계약을 검증한다. |
| P2       | 근거 있는 중복 제거, 순수 로직 분리, 문서 부채                  | 동작을 유지하는 범위에서만 진행한다.       |
| P3       | 장기 구조 개선 후보                                             | 증거와 우선순위가 생길 때까지 기록만 한다. |

새 P0를 발견하면 진행 중인 P1/P2 작업보다 먼저 평가합니다.

## 후보를 선택하는 기준

변경 전 아래 항목을 기록하거나 확인합니다.

1. 구체적인 문제 또는 중복
2. 유지보수·회귀 비용
3. 사용자 동작과 출력 보존 근거
4. 최소 변경 경계
5. 테스트, 타입 검사, 수동 확인 방법

분리 기준은 재사용, 독립 변경 주기, 명확한 책임 경계입니다.

## 코드 구조 규칙

- 순수 변환, 검증, 요청 payload 조립은 함수·모듈로 분리합니다.
- 커스텀 훅은 비즈니스 로직 또는 비동기 상태의 독립된 책임 경계를 기준으로 만듭니다.
- `useMemo`, `useCallback`은 렌더링 비용, referential equality 요구, 의존성 안정화 근거를
  확인한 뒤 사용합니다.
- 화면 상태나 화면 전용 콜백을 캡처하는 테이블 열 정의는 해당 view에 둡니다. 재사용되는
  상태 독립 정적 열 정의는 별도 `columns` 모듈로 관리합니다.
- `packages/ui`에는 web과 admin의 재사용 근거가 있는 UI를 둡니다.

## 디자인 시스템과 UI 규칙

UI 리팩토링은 시각적 동등성을 제품 계약으로 취급합니다. 레이아웃, 반응형 동작,
상태별 화면, 키보드 조작, focus, label, role을 유지합니다.

UI 변경 또는 UI 리팩터링 후보 탐색 전에는 [DESIGN_REVIEW.md](DESIGN_REVIEW.md)를 따라
실제 브라우저 렌더링과 접근성 구조를 검토합니다. 기존 화면과의 시각적 동등성을
확인하면서도, 근거 없는 불일치와 정보 위계 문제는 발견 사항으로 기록합니다.

### 컴포넌트 우선순위

1. `@repo/ui`의 컴포넌트와 variant를 사용합니다.
2. 앱 전용 UI는 해당 앱의 `components/ui` primitive를 사용합니다.
3. 두 앱의 재사용 근거가 있는 UI는 `packages/ui` 추가를 제안합니다.

새 컴포넌트·색상·variant는 컴포넌트와 디자인 토큰의 적용 가능성을 확인한 뒤
추가합니다.

### 색상과 스타일

- 기능 컴포넌트의 색상은 component variant와 의미 기반 토큰
  (`text-text-*`, `bg-surface-*`, `border-border-*` 등)으로 구성합니다.
- primitive 색상 팔레트(`primary-60` 등), CSS 색상 변수, HEX, `rgb()`/`hsl()`,
  임의 색상값의 직접 사용은 금지합니다.
- spacing, radius, shadow, typography는 Tailwind scale과 component variant를
  사용합니다. arbitrary value에는 해당 디자인 제약을 코드 가까이에 기록합니다.
- Canvas, 차트, 외부 라이브러리에는 토큰 적용 가능성을 확인하고 예외 사유를 코드
  가까이에 기록합니다.

색상·간격·문구·variant 변경은 UI 변경으로 분류합니다. 명시적 디자인 요구 또는
사용자 확인을 받은 뒤 진행합니다.

## 테스트와 검증

테스트는 회귀 비용이 큰 계약을 우선 검증합니다.

- 순수 변환과 유효성 검사
- API 요청·응답 경계와 오류 처리
- 권한과 인증 경계
- 빠른 사용자 입력에서 생길 수 있는 비동기 경합
- 로딩·실패·비활성 상태의 핵심 사용자 액션

UI 테스트는 사용자 동작과 접근성 의미에 결합합니다. 제품 코드 변경 시 테스트의
계약 범위를 확인하고 필요한 계약을 추가합니다.

## 실행 순서와 검증 게이트

1. 호출 경로, 사용자 동작, 관련 테스트를 읽는다.
2. 하나의 응집된 경계만 변경한다.
3. 변경 영역 테스트와 타입 검사를 먼저 실행한다.
4. 병합 후보는 `pnpm lint`, `pnpm type-check`, `pnpm test`를 실행한다.
5. 빌드·배포 설정을 바꿨다면 `pnpm build`를 실행한다.
6. UI 변경이 포함되면 `DESIGN_REVIEW.md`에 따라 browser 또는 Storybook에서
   desktop/mobile의 주요 상태와 접근성 구조를 비교한다.

Vercel Preview와 실제 배포 검증은 별도 릴리즈 게이트로 관리합니다. Windows의
standalone symlink 권한 오류는 컴파일·타입·정적 생성 결과와 구분해 기록합니다.

## Git과 기록

- Git의 stage, commit, push, branch, merge는 사용자가 제어합니다.
- 브랜치, PR 승격, 배포와 hotfix 절차는 [GIT_WORKFLOW.md](GIT_WORKFLOW.md)를
  기준으로 관리합니다.
- 하나의 작업은 한 가지 의도를 가진 diff로 유지합니다.
- 각 리팩토링 결과에는 문제, 변경 이유, 사용자 영향, 실행한 검증, 남은 위험을
  기록합니다.
- 장기 지침은 제품 계약과 검증 기준을 기준으로 관리합니다.

## 작업 범위

P2에서는 문서 부채와 근거가 있는 구조 개선을 다룹니다. API 계약은
[FORIF Scalar](https://dev.forif.org/scalar)를 최신 기준으로 확인합니다.
